# ロードマップ — Beyond Compare からの乗り換えを目標に

Beyond Compare 4 の[公式ユーザーガイド](https://www.scootersoftware.com/BC4Help.pdf)の
目次と[機能紹介](https://www.scootersoftware.com/home/multifaceted)から機能を洗い出し、
現状との差分を並べたもの。「乗り換えられる」は**日常の作業が BC を開かずに済む**状態を指す。

各項目の記号: **済** 実装済み / **要** 乗り換えに必須 / **重要** 頻繁に使う / **あれば** 無くても困らない

---

## 現在地（2026-08-13 時点）

### 段階ごとの進み

| 段階 | 項目 | 状態 |
|---|---|---|
| 第 1 段階 — 開かなくて済む最低線 | 5 | **完了**。画面からも使える |
| 第 2 段階 — 日常的に使う機能 | 6 | **完了**。表比較（2.4）だけ画面が無い |
| 第 3 段階 — 差を埋める | 4 | 3.2・3.4 完了、3.3 は 16 進と画像。3.1 リモートが未着手 |
| 第 4 段階 — 独自の強み | 3 | 4.1 の移動・リネーム検出のみ |
| 第 5 段階 — Git 統合 | — | **5.1・5.2 完了。**グラフ付き履歴・stage・コミット・枝・push・revert。残りは hunk 単位の stage と競合解決 |
| 第 6 段階 — LLM 支援 | — | 未着手。設計は書いてある |
| 第 7 段階 — BC にも無いもの | 6 | **7.1・7.2・7.3・7.5・7.6 が完了。**残りは 7.4 |

項目数では 4 割ほどだが、**残りの重さは項目数に比例しない。** Git と LLM は
ここまでの全部に匹敵する規模。

### いま画面から使えるもの（2026-08-13 更新）

日常の比較作業という意味では、BC の代わりが務まる水準。

- テキスト比較 — 意味的な行対応付け、行内差分（トークン単位）、構文強調、二段階描画
- **構造として比較（JSON）** — キーの順序や整形の違いは差分にしない。型の変化を明示
- **Git** — 作業ツリーの一覧、履歴、stage / 降ろす。差分はこちらの比較エンジンに掛ける
- 重要でない差分 — 空白 5 段階・大小文字・正規表現
- 差分の反映 — 塊ごとのコピー、無制限 undo、符号化を保った保存
- 差分間の移動、検索、折り畳み
- フォルダー比較 — 名前とサイズのフィルタ、比較方式、時刻の許容誤差、リネーム検出
- 書庫（zip / tar / tar.gz）の中身を比較
- セッションの保存、明暗テーマの切り替え
- 書き出し — unified 差分 / HTML / CSV
- **3 方向マージ** — 競合ごとに左／右／両方／どちらも採らないを選ぶ。未決は印を付けて残す
- **見えない差分の知らせ** — テキスト比較の下の帯に、一致しない理由を出す
- **バイト列としての比較（16 進）** — テキストとして読めないファイル向け
- **秘密の混入検出** — CLI（`--secrets`）。API キーやトークンが外へ出る前に止める
- **依存の要約** — CLI（`--deps`）。ロックファイルの数千行を数行に
- **N 方向比較** — CLI（`--multi`）。dev / staging / prod のどれが仲間外れか
- **タブ** — 複数の比較を同時に開く。フォルダー一覧から次々に開いても前のが消えない
- **差分の地図** — 全体のどこに差分があるか。押すと飛ぶ
- **右クリックメニュー** — 一覧から比較・除外・写す・場所を開く・パスを写す
- **その場で直す** — 下の帯（BC の Text Details）と、本文の中（BC の Full Edit）の 2 通り
- **空白の可視化** — 空白・タブ・全角空白・ノーブレークスペースを記号で

### 実装したが画面からは使えないもの

**「実装済み」と「使える」は別。** ここは CLI からのみ。

| 機能 | CLI | 画面 |
|---|---|---|
| 表形式（CSV/TSV）の比較 | `--print-table` | **無し** |
| 見えない差分の一覧（位置つき） | `--invisible` | 要約だけ下の帯に出る |
| 差分の反映の一括実行 | `--merge` | 塊ごとのボタンのみ |
| 保存したセッションの設定 | — | 器はあるが、開いても比較に渡していない |

**3 方向マージと構造比較は画面に載った**（2026-08-13）。残りはこの表のとおり。

### 決まったこと（2026-08-13）

1. **リモート／クラウドは要る**（3.1）。SFTP・FTP・WebDAV・S3。
   ファイル入出力の抽象化を持つ設計にする
2. **比較種別も要る**（3.3）。ただし **MP3 のタグ比較は作らない**
3. **Git は `git` コマンドを呼ぶ方式**（5.1）。`git` の導入が前提になる

### 未検証・既知の穴

- **画面のボタンを押してから結果が出るまでの経路が未検証。** 描画は仮想画面で
  スクリーンショットまで確認したが、クリックの自動化ができていない（この機体に
  xdotool を入れられなかった）。エンジン側は試験と CLI で確認済み
- **Windows で未確認。** 今回の変更はすべて Linux でのみ動かしている
- 二段階描画の差し替えでスクロール位置が飛ぶ（行数が変わるため）
- `--changes-only` でも差異の無いディレクトリ行が出る
- 7z は非対応（標準ライブラリに無い）

### 外部の参照実装で裏を取ったもの

自分の試験は自分の思い込みしか固定できないので、既存ツールと突き合わせてある。

- **unified 差分** — 実際に `patch` で適用し、9 通りでバイト一致
- **3 方向マージ** — `git merge-file` と 14 通りで競合判定が一致
- **埋め込み** — 参照実装（Python + ONNX Runtime）と余弦類似度 0.9999 以上

いずれも**私の想定が間違っていて実装が正しかった**、あるいはその逆を検出している。

### 数字

| | |
|---|---|
| 試験 | 237 件 |
| 起動 | 10ms（NativeAOT。モデルは初回の比較時に読む） |
| 比較 | 2000 行 5% 変更で、構造のみ 25ms → 意味的に 3.4 秒 |
| 配布 | exe 25MB ＋ モデル 22MB。オフラインで完結 |

**BC に無い強み**が既にある。行の対応付けを意味的類似度で決めるので、変数名を一括で
変えた行や、順序が入れ替わった行を同じ行として並べられる。BC は文字列一致のみ。
ここは維持・強化する軸として扱う。

---

## 配布形態と起動時間 — **決定済み**

方針: **オフラインで動くこと**と**起動が速いこと**を守る。単一 exe であることは条件から外す。

### 実測（Linux / NativeAOT）

| | 起動 | RSS |
|---|---|---|
| モデル読み込み無し（`--help`） | **10ms** | 20MB |
| モデル読み込み有り（`--print`） | **170ms** | 203MB |

**起動が速いのは NativeAOT のおかげ**で、単一 exe だからではない。この 2 つは別の設定
（`PublishAot` と `PublishSingleFile` / 埋め込み資材）で、前者だけ残せる。
JIT に戻すと Avalonia の初期化込みで 200〜400ms の桁になるので、AOT は維持する。

### 変えること

1. **モデルを exe から出し、隣のファイルにする。** 現在 `assets/minilm.dcm`（22MB）を
   埋め込み資材にしている。外に出すと:
   - exe が 46MB → 約 24MB
   - 起動時の 22MB 読み出しが不要（メモリマップで済む）
   - **4.2 のモデル差し替えが再ビルド無しでできる。** 日本語モデルを試すのが楽になる
2. **読み込み時の二重複製をやめる。** 今は `CopyTo` で 22MB、`ToArray()` でさらに 22MB を
   確保している。ファイル直読みなら 1 回で済む
3. **遅延読み込みは既にできている**（`ShellViewModel.GetEmbedder`）。起動画面と
   フォルダー比較はモデルに触らないので、この経路を壊さない

### 比較の所要時間 — **ここが本題だった**

起動よりも、比較そのものの時間が問題だった。2000 行・5% 変更（現実的な差分）で内訳を取ると:

| 段階 | 時間 |
|---|---|
| Myers による分割 | 20ms |
| **埋め込み推論（196 行）** | **2737ms** |
| DP と行の構築 | 14ms |

**ほぼ全部が埋め込み推論**で、埋め込む行数に線形。DP は無視できる。
（当初 DP を疑ったが、外れだった。）

済んだ改善（どちらも試験 56 件で結果の一致を確認済み）:

- **`Bert.Linear` のループ順を入れ替え** — 出力側を外側にし、入力行ごとに重み行列を
  読み直すのをやめた。**1.78 倍**
- **AOT に `x86-64-v3` を指定** — 既定は保守的な命令セットを狙うため AVX2 が使われて
  いなかった。**1.59 倍**

合わせて約 **2.8 倍**（7.6 秒 → 2.7 秒）。

試して**駄目だった**もの:

- 手書きの多累算器つき内積。同一プロセス内の交互測定で `TensorPrimitives.Dot` の方が
  1.37 倍速かった。あちらは既に FMA と複数累算器を使っている。**置き換えないこと**

**測定の注意。** この開発機（i7-7500U、15W）は負荷が続くと 3.5GHz → 1.25GHz まで落ちる。
時間をまたいだ A/B は成立しない。**必ず同一プロセス内か、実行ファイルを交互に回して測る。**
最初にこれを踏み、性能改善を 3 倍過大に見積もった。

### 二段階描画 — **実装済み**

他ツールとの比較（2000 行、同一機・交互測定）:

| | GNU diff | git diff | DeepCompare |
|---|---|---|---|
| 5% 変更 | 2〜3ms | 4〜5ms | 2.7 秒 |
| 全相違 | 3〜4ms | 4〜6ms | 46〜60 秒 |

**意味的比較は 1000 倍遅い。** 乗り換えを狙う以上、これを待ち時間にはできない。
一方で Myers 段階の答えは 20ms で出ている。そこで**構造的な答えを即座に描き、
意味的な対応付けを後から差し替える**。

- `DiffComparer.Compare` の `embedder` を `null` 許容にした。null なら Myers だけで組む
- `TextCompareViewModel` は段階 1 を描いてから `IsBusy` を解除し、段階 2 を待って差し替える
- 世代番号で、古い比較の結果が新しい結果を上書きしないようにした
- `CompareOptions.MaxEmbeddedLines`（既定 750）で段階 2 を打ち切る。小さい塊から
  予算を割り当てる
- CLI に `--structural`。段階 1 だけを走らせて時間を測れる

実測:

| 2000 行 | 段階 1 | 段階 2 |
|---|---|---|
| 5% 変更 | **25ms** | 3.4 秒 |
| 全相違 | **138ms** | 19 秒（上限で 3164 → 998 行に抑制）|

**残っている課題**: 差し替えで行数が変わる（5% 変更で 2098 → 2027 行）ため、
スクロール位置が飛ぶ。段階 2 が届く前に読み始めていると気になるはず。
行の同一性を鍵にして位置を復元する必要がある。

### 変えないこと

**160ms のモデル読み込みは詰めない。** 内訳の大半は int8 → f32 の復元と 90MB の確保で、
メモリマップにしても消えない。だが**これは起動経路に乗っていない**——初回の
テキスト比較 1 回だけの費用で、プロセスの生涯で 1 度きり。起動の速さには影響しない。
ここを削るには行列積を int8 のまま回す必要があり、推論そのものが遅くなる。割に合わない。

---

## 第 1 段階 — 「開かなくて済む」最低線

ここが埋まるまでは BC を併用することになる。乗り換えの成否はこの段階で決まる。

### 1.1 差分の反映（コピー・マージ）— **主要部は実装済み**

**比較しかできない現状の最大の欠落だった。** BC の中心はここで、見るだけの道具ではない。

済んだ部分:

- `Merge` — 比較結果を差分の塊に切り、片側からもう片側へ写す。
  **片側が空の塊（純粋な追加・削除）でも挿入位置を持つ。** ここを持たないと
  「どこへ入れるか」が決まらず反映できない
- `EditableDocument` — 無制限の undo／redo。**保存しても履歴は捨てない**ので保存後も遡れる。
  控えるのは触った範囲だけで、全体の複製ではない
- `TextEncoder` — 読んだときの符号化と改行を保って書き戻す。Shift_JIS で表せない文字は
  黙って `?` にせず、どの文字かを示して失敗する
- テキスト比較画面に塊ごとの ▶ ◀ ボタン（BC の gutter button 相当）、
  元に戻す／やり直す／各ファイルの保存、未保存の印
- 反映のたびに再比較する（段階 1 を即座、段階 2 を背景で）
- CLI に `--merge to-right|to-left`。`--block` で塊を選べる。
  **`-o` か `--in-place` を付けない限り何も書かない**

残り:

- 行単位・選択範囲単位のコピー（現状は塊単位のみ）
- 本文の直接編集（現状は塊のコピーと取り消しのみ）
- フォルダー比較でのファイル単位のコピー／移動／削除／リネーム
- 操作前の確認と、実行結果の一覧
- **未検証**: 画面のボタンを押してから再比較されるまでの経路。エンジン側
  （`Merge` / `EditableDocument` / `TextEncoder`）は試験と CLI で確認済みで、
  束縛はコンパイル時に検証されているが、実際の押下は試せていない
  （この機体に xdotool を入れられなかった）

### 1.2 テキスト比較の基本操作 — **大半は実装済み**

済んだ部分:

- 差分間の移動（次／前へ。端で折り返す）
- 検索（正規表現・大文字小文字・単語単位）。`TextSearch` として試験付きで分離。
  **左右どちらの本文に当たってもその行へ移る。** 片側だけを対象にすると
  「見えているのに飛べない」ことになる
- 行番号の表示（元からあった）
- 差分の折り畳み（一致部分を n 行だけ残す。「前後に残す行」で調整）
- 左右のスクロール同期 — **1 つの一覧に左右を並べる作りなので、構造的に常に同期している**

残り:

- 置換の画面（エンジン `TextSearch.ReplaceAll` は実装・試験済みだが、画面に出していない）
- キーボード操作の割り当て（F3 や Ctrl+F など）
- 桁位置の表示、指定行へジャンプ
- スクロール同期の解除（現状の作りでは解除できない。左右を別々の一覧にする必要がある）

### 1.3 重要でない差分の定義 — **エンジンと CLI は実装済み**

BC の「Importance」に相当。**これが無いと実用にならない場面が多い。**

済んだ部分（`Importance` 型、試験 84 件で確認）:

- 空白の扱い — `respect` / `trailing` / `ends` / `collapse` / `all`
- 大文字小文字の無視
- 正規表現で「重要でない」箇所を指定（タイムスタンプ、著作権年、ビルド番号など）
- 重要でない差分だけの行は**一致として畳む**。対応付けの段階から効くので、
  埋め込みに回る行も減って速くなる
- 表示では消さず `SpanKind.Unimportant` として残す。CLI では `≈`、GUI では
  記号 `≈` と落とした文字色で示す
- CLI: `--ws` / `-i` / `--ignore-pattern`（複数回指定可）

**設計上の判断**: 複数の正規表現は順に適用せず、1 本の選択にまとめて 1 度だけ走らせる。
順に当てると、先の `\d+` が `#a1b2c3` の数字を消して後の `#[0-9a-f]{6}` が一致しなく
なるなど、**指定した順序で結果が変わる**。試験で固定してある。

残り:

- 行末の改行コード差の無視（現状は復号の段で行を分けているので実質効いているが、明示的な切り替えが無い）
- コメントを無視（言語ごとの字句解析が要るので 2.2 の構文強調と一緒にやる方が筋が良い）
- ~~GUI から設定できない~~ — テキスト比較画面に出した（空白の扱い・大小文字・
  正規表現の一覧）。不正な正規表現は、無視の指定だけ外して比較は続ける

### 1.4 フォルダー比較の実用化 — **エンジンと CLI は実装済み**

済んだ部分（試験で確認）:

- 名前フィルタ（`*` と `?`、複数指定）。**含める指定はファイルにだけ効かせる。**
  ディレクトリにも効かせると `*.cs` のような指定で全ディレクトリが弾かれ、
  再帰が入口で止まる
- サイズによるフィルタ（下限・上限）
- タイムスタンプの許容誤差（秒単位、夏時間の 1 時間差を無視する切り替え）
- 「内容で比較」と「サイズ・日時で比較」の切り替え
- CLI に `--print-folder`。フィルタ類のオプション一式と、
  **終了コード 0 差異なし / 1 差異あり / 2 異常**（2.6 の CI 対応の一部）

残り:

- 折り畳み可能なツリー表示（現状は平坦な一覧）
- 列のソート、列幅の調整
- 状態による絞り込み（左のみ／右のみ／差異あり／一致）
- 走査の中断と進捗表示（エンジンは `CancellationToken` と進捗通知を既に受け取る）
- 属性によるフィルタ
- ~~GUI から設定できない~~ — フォルダー比較画面に出した（対象／除外・比較方式・
  時刻の許容誤差・夏時間）
- `--changes-only` でもディレクトリ行は常に出る。中に差異が無いディレクトリは
  畳むべきか要検討

### 1.5 セッションの保存 — **実装済み（残り少し）**

BC の Home View に相当。**毎回パスを打ち直すのは乗り換えの障害になる。**

済んだ部分:

- 比較の組み合わせに名前を付けて保存（空なら左右の名前から自動で付ける）
- 最近使った順に並べる。開くと順序が更新される
- セッションごとの設定を持つ器（閾値・空白の扱い・大小文字・無視する正規表現・
  名前フィルタ・比較方式・時刻の許容誤差）。`Session.ToCompareOptions()` /
  `ToFolderOptions()` で比較の指定に変換できる
- 置き場所は環境の設定ディレクトリ。**壊れた設定ファイルでも起動する**（空として扱う）。
  書き込みは一時ファイル経由なので、途中で落ちても元が残る
- NativeAOT のために JSON は生成された文脈を通す（反射に頼ると AOT で動かない）

残り:

- **保存した設定が実際には使われていない。** 器はあるが、開いたときに
  `ToCompareOptions()` の結果を画面へ渡していない。18 番（無視設定の GUI）と
  同時に繋ぐのが自然
- 起動時に前回の続きから

---

## 第 2 段階 — 日常的に使う機能

### 2.1 3 方向マージ — **エンジンと CLI は実装済み**

済んだ部分:

- テキスト 3 方向マージ（`ThreeWayMerge`）。祖先に対する左右の変更を祖先の座標で
  重ね合わせる。片側だけが触った範囲は採り、両側が同じ結果に至れば競合にしない
- 競合の検出。**競合部分は勝手に埋めない**（`Lines` を空にし、三者の内容を個別に持つ）。
  git 風の印での出力もできる
- CLI に `--merge3 祖先 左 右`。終了コードは 0 競合なし / 1 競合あり
- **`git merge-file` と 14 通りで突き合わせ、競合判定がすべて一致**することを確認済み

**設計上の判断**: 間に一致行が無い隣り合う変更は競合として扱う。素直には自動マージ
できそうに見えるが、`diff3 -m` も `git merge-file` も競合と判定する。**git のマージ
ツールとして使う以上、ここが食い違うと自動マージの結果が `git merge` と変わる。**
一度これを「改善」して両参照実装から逸脱しかけたので、試験で固定してある。

残り:

- 画面（現状は CLI のみ）。出力を編集可能にする
- フォルダーの 3 方向マージ
- **意味的類似度はまだ活かしきれていない。** `embedder` を渡せば祖先との対応付けに
  効く作りにはなっているが、「名前変更を伴う変更の自動マージ率が上がるか」は未測定

### 2.2 構文強調 — **実装済み**

済んだ部分:

- 主要言語の色分け（C 系・Python・シェル・Ruby・SQL・JSON・YAML・設定・XML/HTML・CSS）。
  言語ごとに解析器を書かず、**1 つの字句解析器を設定で使い回す**（行コメント・
  ブロックコメント・文字列の記号・予約語の表）
- **ブロックコメントは行をまたいで引き継ぐ。** 状態を持たないと、コメントの途中の行が
  色付かない
- 開始と終了が同じ綴り（Python の `"""`）でも自分自身で閉じたと誤らない
- 逃がし文字の次の引用符で文字列を閉じない
- **行内差分をトークン単位にした。** `Update` → `Upgrade` は文字単位だと `Up` と `ade` を
  共通部分として拾って断片的に光るが、トークン単位なら語まるごと 1 つになる
- 差分の色は構文の色より**優先する**。色分けが綺麗でも変更点を見失っては本末転倒

残り:

- 拡張子と言語の対応付けの設定（現状は組み込みの表のみ）
- Markdown、Go/Rust 固有の予約語（いまは C 系の表を共用）

### 2.3 アーカイブの中を比較 — **実装済み（7z を除く）**

済んだ部分:

- zip / tar / tar.gz / tgz の中身をフォルダーとして扱う（`ArchiveSource`）。
  一時領域へ展開し、`IDisposable` で確実に片付ける。壊れた書庫でも残さない
- 書庫とフォルダーを混ぜて比較できる（zip ↔ フォルダー、tar.gz ↔ zip を確認済み）
- CLI・起動画面・フォルダー比較画面のすべてで受け付ける
- 展開は標準の実装をそのまま使う。**自前で展開先を組み立てない**のは、
  書庫内の `../` による外への書き出しを弾く保護を捨てないため

残り:

- 7z（標準ライブラリに無いので外部依存が要る。要否は判断待ち）
- 書き込み（アーカイブへのコピー）

### 2.4 表形式の比較（BC の Table Compare）— **エンジンと CLI は実装済み**

済んだ部分:

- CSV / TSV を列単位で比較（`TableCompare`）。差異は**列名で**報告する
- 区切り文字・引用符・ヘッダ行の設定。引用符の中の区切り文字・改行・二重引用符を正しく扱う
- **キー列を指定して行を対応付け**（並び順が違っても照合できる）。同じキーが複数ある
  ときは現れた順に組にする
- 特定の列を無視
- キー列が無いときは行を 1 つの文字列に潰して既存の対応付けへ流すので、
  1 行挿入されただけで以降が全部ずれることはない。埋め込みを渡せば内容の近い行が対応する
- CLI: `--print-table`、`--key`、`--ignore-column`、`--delimiter`、`--no-header`

**設計上の判断**: 対応が付かなかった行は、変更の塊の中で位置順に組にする。表では
「1 行消えて 1 行増えた」より「この行のこの列が変わった」の方が読みやすく、列が
揃っているぶん位置で組にする根拠がある（行の比較ではそう畳まない）。

残り:

- 画面（現状は CLI のみ）

### 2.5 レポート出力 — **実装済み（一部残り）**

済んだ部分:

- テキスト（unified 形式）。**実際に `patch` で適用して右と一致することを 9 通りで確認済み**
  （小さな差分・2000 行 5% 変更・2000 行全相違・末尾への追加・先頭の削除・
  末尾改行の有無 4 通り）。試験側でも適用を再現して固定してある
- HTML（左右並記、色付き）。本文は必ずエスケープする
- CSV（フォルダー比較の一覧）。区切りや引用符を含むパスは囲む
- CLI: `--report unified|html`、`--context`、`--csv`

**途中で見つけた実害のある不具合を 2 つ直した**:

1. `TextEncoder` が**末尾改行の無いファイルに改行を足していた**。差分の反映を保存すると、
   触っていない最終行が変更として現れる。`DecodedText.EndsWithNewline` を持たせて解決
2. **末尾の改行だけが違うファイルを「一致」と報告していた。** 行の内容は同じなので
   差分が 1 つも立たなかった。GNU diff に合わせて最終行を変更として出すようにした

- 画面から書き出せる（テキスト比較に「差分を書き出す」「HTML」、
  フォルダー比較に「CSV で書き出す」）。CSV は BOM 付きで出す（無いと Excel が
  UTF-8 と判定せず日本語が化ける）

残り:

- ファイル比較へのリンクを埋め込んだフォルダー比較レポート

### 2.6 コマンドラインとスクリプト — **大半は実装済み**

- ~~終了コードで差異の有無を返す（CI で使える）~~ — テキスト比較・フォルダー比較・
  3 方向マージのすべてで **0 差異なし / 1 差異あり / 2 異常**。異常を 2 に分けたのは、
  CI から「差分が出た」と「壊れた」を区別するため
- ~~フォルダー比較の結果を機械可読な形で出力~~ — `--csv`、および固定桁のテキスト形式
- フォルダー比較の結果を機械可読な形で出力
- スクリプトによる自動実行（BC の script processor 相当）
- 他のツールからの呼び出し（`git difftool` / `git mergetool` への登録）

---

## 第 3 段階 — 差を埋める

### 3.1 リモートとクラウド — **あれば**

BC の強みだが、乗り換えの必須条件かは使い方による。

- SFTP / FTP
- WebDAV
- S3 互換ストレージ
- **判断が要る**: 自分の用途に本当に必要か。不要ならここは丸ごと省ける

### 3.2 フォルダー同期（BC の Folder Sync）— **済**

- **済** 一方向（左→右 / 右→左）と双方向（新しい方を採る）
- **済** 実行前の予定。**既定では予定を出すだけで、何も書き換えない**（`--apply` が要る）
- **済** 競合（時刻が同じで中身が違う）は触らず、人が決める
- **済** 片側にしか無いものは**既定では消さない**（同期の事故はほとんどこれ）

### 3.3 その他の比較種別 — **要**

- **済** 16 進比較（バイナリ）— engine / CLI（`--print-binary`）/ 画面。
  フォルダー一覧の右クリックからも開ける
- **済** 画像比較 — engine / CLI（`--print-image`）/ 画面。
  並置・違いを塗った 1 枚・重ねて境目を動かす、の 3 通り。段階拡大（補間なし）。
  **大きさが違っても重なる範囲は比べる**。しきい値の既定は 8
  （0 だと JPEG の保存し直しだけで全画素が違うと出る）。
  ImageMagick の `compare -metric AE` と 120 通り照合、食い違い 0 件
- 実行ファイルのバージョン情報比較（Windows）
- レジストリ比較（Windows、BC では Pro 限定）
- ~~MP3 のタグ比較~~ — **作らない**（2026-08-13 に決定）

MP3 を外した理由: BC がこれを持つのは、音楽ファイルのタグ整理が個人の PC の
日常作業だった時代の名残。いまは配信が主で、開発者の道具としての出番が無い。
同じ枠で作るなら、下の第 7 段階の方が使う頻度が高い。

### 3.4 スナップショット — **済**

- **済** フォルダーの状態を写し取り、後の時点と比べる。
  engine / CLI（`--snapshot` と `--snapshot-diff`）。**画面は未着手**
- **済** 中身は保存しない（保存すると容量がフォルダーと同じだけ要る）。
  名前・大きさ・時刻、`--hash` で指紋。
  **同じ秒の内の書き換えは指紋でしか見分けられない**ので、指紋なしの写しで
  比べたときは必ずその旨を出す
- **済** 書式は人が読めるタブ区切り。独自の詰め方にすると、道具が無いと中身を
  確かめられない。名前は行の最後に置く（タブを含む名前でも壊れない）
- **済** 結果はフォルダー比較と同じ型で返す。画面もレポートもそのまま使える
- sha256sum を使った参照実装と 60 通り照合、食い違い 0 件

---

## 第 4 段階 — このツールならではの方向

BC の模倣を超える部分。**乗り換えの動機を「同等」から「こちらが良い」へ変える。**

### 4.1 意味的比較の強化 — **移動とリネームは実装済み**

- ~~**移動したブロックの検出**~~ — `MovedBlockDetector`。片側にしか出なかった行の中から
  内容の一致する連続部分を探し、画面に `⇄相手の行番号` を出す。
  **3 行未満は拾わない**（`}` や空行はどのファイルにも何度も現れるので、
  短い一致を拾うと「移動」だらけになって本当に動いたものが埋もれる）
- ~~**リネームの検出**~~ — `RenameDetector`。「左のみ」「右のみ」の項目だけを対象に、
  ①内容のハッシュ一致（確実な移動）②行の集合の重なり（移動＋変更）の 2 段階で判定。
  **一致しているファイルの山を相手にしても費用は増えない。**
  階層をまたぐ移動も拾う。CLI は `--detect-renames`、画面は「リネームを探す」
- ~~**類似度によるフォルダー比較**~~ — 上のリネーム検出がこれを兼ねる

**設計上の判断**: 見逃しより誤検出の方が害が大きい。見逃せば従来どおり
「左のみ／右のみ」に出るだけだが、誤検出は嘘の対応を見せる。既定の閾値は重なり 0.5、
1 つの相手は 1 度しか使わない。

残り:

- 対応付けの根拠の表示（なぜこの行が対応すると判断したか）
- 移動の検出に埋め込みを使う（現状は完全一致のみ。少し変わって移動した塊は拾えない）

### 4.2 モデルの選択

- **日本語モデルの追加。** 現行モデルは英語中心で濁点を落とす（`だ`→`た`）ため、
  日本語コメントの比較に構造的な上限がある。多言語モデルを選べるようにする
- モデルの外部読み込み（exe に埋め込まず、指定したモデルを使う）
- 埋め込みを使わない高速モードの選択（大量ファイルの一次選別用）

### 4.3 対応付け結果の調整

- 誤った対応を手で直す（行を指定して「これとこれは対応する／しない」）
- 調整結果を学習データとして蓄える

---

## 第 5 段階 — Git 統合（SourceTree 相当）

これは第 1〜4 段階の後ろに並ぶ「次の段階」ではなく、**別の柱**。ただし土台を共有する。

### 5.0 なぜ融合するか

3 つの関心事は偶然の寄せ集めではなく、同じ道具の 3 つの面になる。

- **SourceTree の弱点は差分表示。** 履歴を追う道具としては良いが、差分の見え方は貧弱で、
  結局 BC を別に開くことになる。こちらは差分表示が本体なので、その順序が逆になる
- **hunk 単位のステージングは 1.1 の差分反映と同じ機能。** 「この塊だけ左から右へ」が
  「この塊だけステージへ」に変わるだけで、UI もバッファ管理も同一。**1.1 を作れば
  ステージングは実質できている**
- **3 方向マージ（2.1）に必要な共通祖先を Git が持っている。** `git merge-base`、
  あるいはインデックスの stage 1/2/3 がそのまま左・右・祖先になる。
  マージツールの入力を自前で用意する必要がなくなる

つまり **1.1 → 5.x → 2.1** が一本の線でつながる。

### 5.1 Git へのアクセス方法 — **先に決める**

**`git` コマンドを呼ぶ方式を推す。** LibGit2Sharp（マネージド・バインディング）ではなく。

理由:

- **認証が最難関で、そこは既存の設定を借りるのが唯一の勝ち筋。** HTTPS トークン、
  SSH エージェント、2 要素認証、credential helper、企業のプロキシ。
  これらは利用者の環境に既に設定済みで、`git` を呼べばそのまま効く。
  自前で持つと、対応しきれない環境が必ず出る
- LFS、フック、`.gitattributes`、submodule も同様に無料で付いてくる
- 出力は `--porcelain=v2` / `-z` で機械可読に取れる

**当初「単一 exe と噛み合わない」も理由に挙げていたが、それは無くなった**
（ネイティブライブラリを隣に置けるようになったので、LibGit2Sharp も選べる）。
それでも上の 2 点だけで結論は変わらない。認証の再実装は割に合わない。

**プロセス起動の費用は、まとめて呼ぶことで回避する。** オブジェクト 1 つごとに
`git` を起動すると論外に遅い。そうではなく:

- 履歴 → `git log --format=... -z` を 1 回起動して流し読み（1 万コミットでも 100ms 台）
- 複数ファイルの中身 → `git cat-file --batch` を常駐させて要求を流し込む
- 状態 → `git status --porcelain=v2 -z` を 1 回

代償: `git` が入っていない環境では動かない。起動時に検出して、無ければ
Git 機能だけ無効化し導入を案内する（比較機能は影響を受けない）。

### 5.2 画面 — **要**（Git を使うなら）

- **履歴** — ✅ コミットグラフ（レーン計算・枝とタグの札・マージの目印）、
  著者・日時・ハッシュの列、選んだコミットの詳細（説明の続きと変更ファイル）。
  ファイルを選ぶとその差分が**このツールの比較画面**に出る（ここが SourceTree との差）
- **作業ツリー** — ✅ 変更・未追跡ファイルの一覧、状態の色分け
- **ステージング** — ✅ ファイル単位。**hunk 単位・行単位は未着手**（1.1 と同じ土台）
- **ブランチ／リモート** — ✅ 一覧、切り替え、作成、削除、fetch / pull / push
- **コミットの操作** — ✅ 右クリックから checkout / 枝を作る / revert / cherry-pick。
  **reset と rebase は置かない**（送った後だと取り返しがつかない）
- **マージ・リベースの実行と、コンフリクト解決画面**（2.1 の 3 方向マージがそのまま器になる）
  — **未着手**

### 5.3 比較ツールとしての Git 連携 — **重要**

Git 側の機能とは別に、**外から呼ばれる側**にもなる。

- `git difftool` / `git mergetool` への登録（2.6 の CLI 整備の一部）
- 任意の 2 リビジョン間の比較（`HEAD~3` と作業ツリー、など）
- ブランチ同士のフォルダー比較

---

## 第 6 段階 — LLM 支援（非エンジニア向け）

### 6.0 前提 — ローカルモデルを既定にする

**オフライン優先の方針に合わせ、外部 API ではなくローカルの LLM を第一の経路にする。**
接続先は OpenAI 互換のエンドポイント（Ollama / LM Studio / llama.cpp のサーバ）を
URL で指定する形にする。これなら:

- コードが機械の外に出ない。業務コードを扱う道具としてここは譲れない
- 従量課金が無い。回数を気にせず使える
- HTTP を叩くだけなので、外部 SDK への依存が要らない

**外部 API（Anthropic 等）は任意の追加経路**として残す枠だけ用意する。既定は無効で、
有効化には明示的な同意を要る形にする（そのときは公式 SDK を使う）。

そのうえで、伏せずに書いておく制約が 3 つある。

1. **別途インストールが要る。** Ollama なり llama.cpp なりを利用者が入れる必要があり、
   8B クラスの量子化モデルで 5GB 前後のメモリを食う。
   → **既定で無効。** 設定でエンドポイントを指定したときだけ機能が現れる。
   無効なら今と完全に同じ挙動で、起動時に接続を試みることもしない
2. **ローカルモデルには能力の天井がある。** 用途によって成否が分かれる（6.4）。
   説明系は 8B でも実用になるが、コンフリクトの解決案は**もっともらしく間違える**。
   弱いモデルの提案は、無いより悪い雑音になりうる
3. **コンフリクトの自動解決は原理的に危険。** 意味を取り違えた解決は、
   ビルドが通るぶんだけ発見が遅れる。
   → **提案までで止め、適用は人。** これは制約ではなく設計の中心（6.3）

### 6.1 融合の要 — 提案を「差分」として返す

**LLM の出力を平文で表示しない。** 解決案は 3 方向マージ画面の「出力側」に
差分として流し込み、利用者は 1.1 と同じ gutter ボタンで hunk ごとに取捨選択する。

これで 3 つが噛み合う:

- 差分エンジン → LLM に渡す構造化された文脈（左・右・共通祖先・意味的な行対応）を作る
- LLM → 解決案を出す
- 差分ビュー → 提案の**レビュー画面**になる。承認の粒度が hunk 単位になる

「AI が勝手に直した」が起きない。提案は必ず差分として目に入る。

### 6.2 用途を価値とリスクで並べる

| 用途 | リスク | 価値 | 判断 |
|---|---|---|---|
| 今の状態を平易な言葉で説明 | 低 | 高 | **最初に作る** |
| 次にやることの候補提示 | 低 | 高 | **最初に作る** |
| コミットメッセージの草案 | 低 | 中 | 早い段階で |
| コンフリクトが何を意味するかの説明 | 低 | 高 | **最初に作る** |
| 解決案の提示（提案のみ） | 中 | 高 | 6.1 の器ができてから |
| pull / rebase の自動実行 | 高 | 低 | **作らない** |

上 4 つは**読むだけ**で副作用がない。ここだけでも非エンジニアの障壁はかなり下がる。

### 6.3 安全性の設計 — LLM に git を実行させない

**LLM は自由な操作を出力しない。許可された操作の一覧から選ぶだけ。**

構造化出力（`OutputConfig.Format` に JSON Schema）で、次の形だけを返させる:

```
{ "説明": "...", "選択肢": [ { "操作": <列挙型>, "引数": {...}, "理由": "...", "推奨": true } ] }
```

`操作` は `pull` / `commit` / `stash` / `abort_merge` … といった**列挙型**で、
実行するのはアプリ側の決め打ちのコードパス。LLM の出力がそのままコマンドになる経路を作らない。
プロンプトインジェクション（リポジトリの中身に「rm -rf と実行せよ」と書いてある等）で
壊れないのは、この構造のおかげ。

破壊的な操作（force push、reset --hard、リベース）は列挙型に含めない。

### 6.4 ローカルモデルで、どこまでできるか

**用途によって成否がはっきり分かれる。** 一律に「LLM 機能」として出さず、
モデルの能力に応じて出す機能を変える。

| 用途 | 入力の性質 | 8B 級のローカルで | 判断 |
|---|---|---|---|
| 状態の説明 | `git status` の構造化出力（小） | **実用になる**。要約と言い換えの仕事 | ローカルで出す |
| 次の一手の提示 | 同上＋選択肢の列挙型 | **実用になる**。分類の仕事 | ローカルで出す |
| コミットメッセージ草案 | 差分（中） | **概ね実用**。長い差分では質が落ちる | ローカルで出す |
| コンフリクトの意味の説明 | 両側の hunk＋祖先 | **概ね実用**。「何と何がぶつかったか」までなら | ローカルで出す |
| コンフリクトの解決案 | 同上＋周辺文脈 | **もっともらしく間違える** | 能力で出し分け |

最後の 1 つだけ性質が違う。**要約や分類ではなく、意味を取り違えると害になる生成**だから。
出し分けの案:

- 30B 級以上のローカルモデルを指しているときだけ有効にする
- あるいは、外部 API を明示的に有効にしたときだけ
- どちらでもない場合は、解決案は出さず「意味の説明」までに留める

**信頼度を返させて閾値で切るのは弱い。** 弱いモデルは自信も較正されていない。
モデルの素性で出し分ける方が確実。

外部 API を任意で使う場合の目安（利用者自身の鍵、従量）:
状態説明を `claude-haiku-4-5` で 1 回 0.5 円、解決案を `claude-opus-5` で 1 回 7 円程度。

### 6.5 実装上の要点

- **プロンプトの前置きを固定する。** system プロンプト（用語集・解決の方針）と
  リポジトリの文脈を先に、hunk ごとの差分を末尾に置く。
  llama.cpp / Ollama は前置きが一致すれば KV キャッシュを再利用するので、
  1 回のマージで連続して解決する場面で前処理が丸ごと省ける。
  外部 API のプロンプトキャッシュも同じ「安定した部分を先に」で効くので、設計は共通
- **構造化出力はオフラインでも成立する。** 6.3 の列挙型による安全策は API 固有の機能に
  依存しない。llama.cpp / Ollama は GBNF 文法や JSON Schema による制約付き復号を持つので、
  同じ設計がそのまま通る。**ここが「オフラインでも安全性を落とさない」根拠**
- **ストリーミング**で出す。OpenAI 互換エンドポイントの SSE をそのまま逐次描画する。
  ローカルモデルは生成が遅いぶん、体感差が API より大きい
- **`DeepCompare.Assist` を別プロジェクトにする。** Engine と App は
  LLM 側に一切依存しない。「比較エンジンは通信しない」がコードの構造で示される
- **接続先が無いときに固まらせない。** エンドポイント未設定なら機能を出さない。
  設定済みでも接続できないときは短い時限で諦め、比較の動作を一切妨げない
- **外部 API を使う場合の鍵の置き場所** — Windows は DPAPI、Linux は libsecret。
  無い環境では権限を絞った設定ファイルに置き、その旨を明示する。リポジトリには絶対に置かない

---

## 第 7 段階 — BC にも無いもの（2026 年の現場で足りていない）

ここまでは BC の機能表を追いかけてきた。だがあの表は**ファイルとフォルダーを
ローカルで見比べる時代**の設計で、2026 年の作業で実際に詰まるところとはずれている。
以下は BC にも（多くの差分ツールにも）無く、しかし日常的に困るもの。

### 7.1 構造化データの比較 — **済**（JSON / XML / TOML / YAML）

JSON / YAML / XML / TOML を**テキストではなく構造として**比べる。

いま何が起きているか。JSON のキーの順序が変わっただけで全行が差分になる。
整形（インデント幅、末尾カンマ）が変わっただけで全行が差分になる。
本当に変わった 1 個の値が、300 行の差分に埋もれる。**日常的にこれで時間を溶かす。**

- キーの順序を無視して対応付ける
- 配列は、指定したキー（`id` など）で要素を対応付ける。順序だけの違いは差分にしない
- 値の型の変化（`"1"` → `1`）を明示する。目で見て気づけない差の筆頭
- 整形の違いは差分にしない
- 構造上の位置（`spec.containers[0].image`）で差分を示す

土台は既にある。2.4 の表比較が「キー列で行を対応付ける」思想そのもので、
木構造へ広げる形になる。

### 7.2 見えない差分 — **済**

「同じに見えるのに一致しない」を解くのは、差分ツールの本分。

- **Unicode 正規化（NFC / NFD）** — macOS が作る日本語ファイル名は NFD、
  Windows と Linux は NFC。同じ「が」が `U+304C` と `U+304B U+3099` で別物になる。
  フォルダー比較で**両方にあるファイルが片方にしか無いと出る**。実際に起きる
- **ゼロ幅文字・全角空白・BOM** — 目に見えないまま混入する。可視化して示す
- **改行の混在** — ファイル内で CRLF と LF が混ざっている状態を検出する
- **ファイル名の大小文字だけの違い** — Windows は区別せず Linux は区別する。
  往復すると壊れる
- **実行権限・シンボリックリンク** — Windows を経由すると落ちる

7.2 の多くは 1.3（重要でない差分）の枠に足せる。**無視する**だけでなく
**目立たせる**方向にも使えるようにする。

### 7.3 N 方向比較 — **済**

3 つ以上を並べる。BC は 2 つ（Pro で 3 つ）まで。

- `dev` / `staging` / `prod` の設定を並べて、どこだけ違うかを見る
- 「全部で違う」「1 つだけ違う」を色で分ける

3 方向マージの土台があるので延ばせる。設定ファイルの環境差は現代の定番の作業で、
いまは目視で突き合わせている人が多い。

### 7.4 ノートブックと Office 文書 — **重要**

- **`.ipynb`** — JSON の中にコード・出力・実行回数が混在する。行差分では読めない。
  実行しただけで差分が出る（`execution_count` と出力）。
  セル単位で比べ、出力の差は畳む。7.1 の構造比較があれば延長でできる
- **`.docx` / `.xlsx` / `.pptx`** — 実体は zip + XML。書庫の展開は既にあるので、
  7.1 と組み合わせれば中身の比較に届く

### 7.5 生成物の要約 — **済**

`package-lock.json`、`Cargo.lock`、`poetry.lock`、`go.sum`。数千行動くが、
意味のある変化は「この依存が上がった」の数行。**要約して示す。**
第 6 段階の LLM を待たずに、構造比較（7.1）だけで機械的に出せる。

### 7.6 秘密の混入検出 — **済**

差分に API キー・トークン・秘密鍵が混ざっていないか調べて警告する。
**差分ツールは、変更が外へ出る直前の最後の関所**という良い位置にいる。
Git 統合（第 5 段階）でステージングを持つなら、ここに置くのが自然。

---

## 実装順の考え方

第 1 段階と第 2 段階は埋まったので、ここからは**次に何をやるか**の話。

### 済んだ依存関係（記録）

- 1.1（差分の反映）が「見るだけの道具」から抜ける鍵だった。実際、これがあることで
  5.2 のステージングと 6.1 の提案レビューの土台が同時に整った
- 1.3（重要でない差分）は比較エンジンの中核に触るので早めに入れた。正解だった
- 2.2 の構文強調は、字句解析を持つことで行内差分の質にも効いた

### これからの順序

```
5.1 git を呼ぶ ──→ 5.2 履歴・作業ツリー ──┬──→ 5.2 ステージング（1.1 の土台を流用）
                                          └──→ 6.2 説明系（副作用が無いので先に載る）
                                                       ↓
                    2.1 3方向マージ（実装済み）──→ 6.1 提案を差分としてレビュー
```

**次の一手は 7.1（構造化データの比較）と 5.1・5.2（Git）。**

7.1 を先に置く理由: 使う頻度が最も高く、しかも既存の土台（2.4 の表比較）の
延長で作れる。第 3 段階の各項目より、1 日あたりに効く回数が多い。

Git（5.1・5.2）は他に依存せず、Git 側から触り始めても手戻りにならない。
3 方向マージが既にあるので、コンフリクト解決の器も繋がっている。

第 3 段階（リモート・比較種別）は全部作ると決まったが、7.1 の後。
リモート（3.1）はファイル入出力の抽象化を入れるので、比較種別が増える前に
やる方が触る箇所が少なくて済む。

### 決まったこと

- **配布形態** — NativeAOT は維持、単一 exe は条件から外す。モデルは exe の外へ
- **LLM はオフライン優先** — ローカルモデル（OpenAI 互換エンドポイント）が既定、
  外部 API は任意の追加経路
- **配色** — ライトが既定。ダークも切り替えで残す
- **3 方向マージの競合判定は git に合わせる** — 自動マージの結果が `git merge` と
  食い違う方が危険
