[package]
name = "deepcompare-model-prep"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false

[[bin]]
name = "model-prep"
path = "src/main.rs"

[dependencies]
anyhow.workspace = true
# 保存形式の定義はエンジン側が正本。ここから参照して、書き出しと読み込みが
# 食い違わないようにする。
deepcompare-engine = { path = "../engine" }
half.workspace = true
safetensors.workspace = true
