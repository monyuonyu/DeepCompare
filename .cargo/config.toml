# Linux から Windows 向けの exe を作るための設定。
# mingw-w64 を使う（MSVC ABI ではなく GNU ABI）。導入は:
#   sudo apt-get install gcc-mingw-w64-x86-64
[target.x86_64-pc-windows-gnu]
linker = "x86_64-w64-mingw32-gcc"
ar = "x86_64-w64-mingw32-ar"
