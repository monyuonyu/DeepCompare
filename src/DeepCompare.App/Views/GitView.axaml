<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:DeepCompare.App"
             x:Class="DeepCompare.App.Views.GitView"
             x:DataType="vm:GitViewModel">

  <UserControl.Resources>
    <!--
      作業ツリーの 1 行。**stage 済みと未 stage で同じ形を使う。**
      置き場所（どちらの見出しの下か）で意味が決まるので、行の中で
      色を分ける必要はない。
    -->
    <DataTemplate x:Key="GitFileTemplate" x:DataType="vm:GitFileRow">
      <Border Classes="row" Background="Transparent">
        <Grid ColumnDefinitions="20,*,Auto">
          <!-- **名前が先、場所は薄く後ろ。** VS Code と同じ並び -->
          <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8"
                      ToolTip.Tip="{Binding Path}">
            <TextBlock Text="{Binding FileName}" VerticalAlignment="Center" />
            <TextBlock Text="{Binding Directory}" FontSize="11" VerticalAlignment="Center"
                       Foreground="{DynamicResource FgDim}"
                       TextTrimming="CharacterEllipsis" />
          </StackPanel>

          <!-- 状態は右端に 1 文字。git の 2 文字はツールチップに残す -->
          <TextBlock Grid.Column="0" Text="{Binding Mark}" Classes="mono"
                     FontWeight="Bold" VerticalAlignment="Center"
                     HorizontalAlignment="Center"
                     Foreground="{Binding MarkBrush}"
                     ToolTip.Tip="{Binding KindLabel}" />

          <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="0">
            <!--
              競合しているファイルにだけ出す。**押せない状態で置かない。**
              3 方向マージの画面をそのまま器にする。
            -->
            <Button Classes="accent" Padding="7,2" Margin="0,0,4,0"
                    IsVisible="{Binding Status.IsConflicted}"
                    ToolTip.Tip="祖先・こちら・むこうを並べて解決します"
                    Command="{Binding $parent[UserControl].((vm:GitViewModel)DataContext).ResolveCommand}"
                    CommandParameter="{Binding}">
              <StackPanel Orientation="Horizontal" Spacing="4">
                <Path Classes="icon" Data="{StaticResource IconMerge}"
                      Width="13" Height="13" VerticalAlignment="Center" />
                <TextBlock Text="解決" FontSize="11" VerticalAlignment="Center" />
              </StackPanel>
            </Button>
            <Button Classes="icon small" ToolTip.Tip="HEAD と作業ツリーを比べる"
                    Command="{Binding $parent[UserControl].((vm:GitViewModel)DataContext).OpenFileCommand}"
                    CommandParameter="{Binding}">
              <Path Classes="icon" Data="{StaticResource IconOpen}" />
            </Button>
            <Button Classes="icon small" IsVisible="{Binding CanStage}"
                    ToolTip.Tip="塊ごとに索引へ載せる（git add -p に当たる）"
                    Command="{Binding $parent[UserControl].((vm:GitViewModel)DataContext).StagePartlyCommand}"
                    CommandParameter="{Binding}">
              <Path Classes="icon" Data="{StaticResource IconStagePartly}" />
            </Button>
            <Button Classes="icon small" IsVisible="{Binding CanStage}"
                    ToolTip.Tip="索引へ載せる（stage）"
                    Command="{Binding $parent[UserControl].((vm:GitViewModel)DataContext).StageCommand}"
                    CommandParameter="{Binding}">
              <Path Classes="icon" Data="{StaticResource IconStage}" />
            </Button>
            <Button Classes="icon small" IsVisible="{Binding CanUnstage}"
                    ToolTip.Tip="索引から降ろす。中身は変えない"
                    Command="{Binding $parent[UserControl].((vm:GitViewModel)DataContext).UnstageCommand}"
                    CommandParameter="{Binding}">
              <Path Classes="icon" Data="{StaticResource IconUnstage}" />
            </Button>
          </StackPanel>
        </Grid>
      </Border>
    </DataTemplate>
  </UserControl.Resources>

  <Grid RowDefinitions="Auto,*,Auto">

    <!-- 上の帯。操作はアイコン、場所は 1 行 -->
    <Border Grid.Row="0" Classes="topbar">
      <StackPanel Orientation="Horizontal" Spacing="6">
      <Button Classes="icon" Command="{Binding Shell.GoHomeCommand}"
              ToolTip.Tip="起動画面へ戻る（比較の状態は残ります）">
        <Path Classes="icon" Data="{StaticResource IconHome}" />
      </Button>
      <Border Classes="vsep" />
        <Button Classes="icon" Command="{Binding RefreshCommand}" ToolTip.Tip="読み直す">
          <Path Classes="icon" Data="{StaticResource IconRefresh}" />
        </Button>
        <Border Classes="vsep" />

        <Path Classes="icon dim" Data="{StaticResource IconGit}" VerticalAlignment="Center" />
        <TextBlock Text="{Binding Branch}" VerticalAlignment="Center"
                   Foreground="{DynamicResource Accent}" FontWeight="SemiBold" />

        <Border Classes="vsep" />
        <Button Classes="icon" Command="{Binding FetchCommand}"
                ToolTip.Tip="遠隔の状態を取ってくる（fetch）">
          <Path Classes="icon" Data="{StaticResource IconSave}" />
        </Button>
        <Button Classes="icon" Command="{Binding PullCommand}"
                ToolTip.Tip="遠隔の変更を取り込む（pull --ff-only）">
          <Path Classes="icon" Data="{StaticResource IconStage}" />
        </Button>
        <Button Classes="icon" Command="{Binding PushCommand}"
                ToolTip.Tip="こちらの変更を送る（push）">
          <Path Classes="icon" Data="{StaticResource IconUnstage}" />
        </Button>

        <Border Classes="vsep" />
        <TextBox Text="{Binding Path}" Width="330"
                 PlaceholderText="リポジトリの中のどこか"
                 ToolTip.Tip="{Binding Root}" />
        <ProgressBar IsIndeterminate="True" IsVisible="{Binding Busy}"
                     Width="70" VerticalAlignment="Center" />
      </StackPanel>
    </Border>

    <!-- 左に作業ツリー、右に履歴 -->
    <Grid Grid.Row="1" ColumnDefinitions="*,8,*" Margin="12,8">

      <!--
        作業ツリー。**VS Code の並びに寄せる。**
        上から順に、コミットの説明 → 索引に載っているもの → 載っていないもの。
        以前は説明が一番下、ファイルは 1 つの一覧に混ぜて色で示していたので、
        「次のコミットに何が入るのか」が一目で分からなかった。
      -->
      <DockPanel Grid.Column="0">

        <!--
          コミット。**索引に何も載っていなければ押せない。**
          押しても git が断るだけなので、その前に分かる方がよい。
        -->
        <StackPanel DockPanel.Dock="Top" Spacing="6" Margin="0,0,0,10">
          <TextBox Text="{Binding CommitMessage}" AcceptsReturn="True" Height="56"
                   PlaceholderText="このコミットで何をしたか" />
          <StackPanel Orientation="Horizontal" Spacing="6">
            <Button Classes="accent" Command="{Binding CommitCommand}">
              <StackPanel Orientation="Horizontal" Spacing="6">
                <Path Classes="icon" Data="{StaticResource IconCheck}"
                      Width="13" Height="13" VerticalAlignment="Center" />
                <TextBlock Text="コミット" VerticalAlignment="Center" />
              </StackPanel>
            </Button>
            <Button Content="直前を書き直す" Command="{Binding AmendCommand}"
                    ToolTip.Tip="空のままなら、元の説明をそのまま使います" />
            <!--
              **接続先を設定した人にだけ出る。** 設定していない人にとって、
              この機能は存在しないのと同じでよい。
            -->
            <Button Content="草案をもらう" Command="{Binding Assist.DraftCommitCommand}"
                    IsVisible="{Binding Assist.IsAvailable}"
                    ToolTip.Tip="stage された差分から説明の草案を書きます。そのまま記録はしません" />
            <Button Content="いまの状態を説明" Command="{Binding Assist.ExplainCommand}"
                    IsVisible="{Binding Assist.IsAvailable}" />
          </StackPanel>

          <!--
            支援の返事。**畳んだままにしない。** 出るときは必ず見える場所に置く。
          -->
          <Border IsVisible="{Binding Assist.HasExplanation}"
                  Background="{DynamicResource SubtleBg}"
                  CornerRadius="{DynamicResource RadiusSmall}" Padding="8,6">
            <StackPanel Spacing="4">
              <TextBlock Text="{Binding Assist.Explanation}" TextWrapping="Wrap"
                         FontSize="12" />
              <ItemsControl ItemsSource="{Binding Assist.Suggestions}">
                <ItemsControl.ItemTemplate>
                  <DataTemplate>
                    <Grid ColumnDefinitions="Auto,*" Margin="0,2,0,0">
                      <TextBlock Grid.Column="0" Text="{Binding Display}" FontSize="12"
                                 FontWeight="SemiBold" Margin="0,0,8,0" />
                      <TextBlock Grid.Column="1" Text="{Binding Reason}" FontSize="12"
                                 TextWrapping="Wrap"
                                 Foreground="{DynamicResource FgDim}" />
                    </Grid>
                  </DataTemplate>
                </ItemsControl.ItemTemplate>
              </ItemsControl>
              <!--
                **押せるボタンにしない。** 提案までで止め、実行は人が
                普段の操作でやる。並べたものを押せるようにすると、
                「AI が勧めたから」で作業ツリーが動く。
              -->
              <TextBlock Text="提案までです。実行するかどうかは、上のボタンから自分で決めてください。"
                         IsVisible="{Binding Assist.Suggestions.Count}"
                         FontSize="11" Foreground="{DynamicResource FgDim}" />
            </StackPanel>
          </Border>
        </StackPanel>

        <!--
          変更が無いときの案内。**空の枠をそのまま見せない。**
          何も無いのか、まだ読んでいないのかが区別できない。
        -->
        <Panel>
          <StackPanel IsVisible="{Binding IsClean}" Spacing="8"
                      HorizontalAlignment="Center" VerticalAlignment="Center">
            <Path Classes="icon" Data="{StaticResource IconFilterSame}"
                  Width="34" Height="34" HorizontalAlignment="Center"
                  Fill="{DynamicResource FgDisabled}" />
            <TextBlock Text="変更はありません" HorizontalAlignment="Center"
                       Foreground="{DynamicResource FgDim}" />
          </StackPanel>

          <ScrollViewer>
            <StackPanel Spacing="4">

              <!-- 索引に載っているもの＝次のコミットに入るもの -->
              <StackPanel IsVisible="{Binding HasStagedFiles}" Spacing="2">
                <StackPanel Orientation="Horizontal" Spacing="8" Margin="2,4,0,2">
                  <TextBlock Text="次のコミットに入るもの" Classes="title" FontSize="12" />
                  <Border Background="{DynamicResource CardBg}" CornerRadius="8"
                          Padding="6,1" VerticalAlignment="Center">
                    <TextBlock Text="{Binding StagedCount}" FontSize="11" />
                  </Border>
                </StackPanel>
                <ItemsControl ItemsSource="{Binding StagedFiles}"
                              ItemTemplate="{StaticResource GitFileTemplate}" />
              </StackPanel>

              <!-- 索引に載っていないもの -->
              <StackPanel IsVisible="{Binding HasUnstagedFiles}" Spacing="2">
                <StackPanel Orientation="Horizontal" Spacing="8" Margin="2,8,0,2">
                  <TextBlock Text="入らないもの" Classes="title" FontSize="12" />
                  <Border Background="{DynamicResource CardBg}" CornerRadius="8"
                          Padding="6,1" VerticalAlignment="Center">
                    <TextBlock Text="{Binding UnstagedCount}" FontSize="11" />
                  </Border>
                </StackPanel>
                <ItemsControl ItemsSource="{Binding UnstagedFiles}"
                              ItemTemplate="{StaticResource GitFileTemplate}" />
              </StackPanel>

            </StackPanel>
          </ScrollViewer>
        </Panel>
      </DockPanel>

      <GridSplitter Grid.Column="1" Background="{DynamicResource Divider}" Width="2" />

      <DockPanel Grid.Column="2">
        <TextBlock DockPanel.Dock="Top" Text="枝" Classes="title" Margin="0,0,0,6" />
        <Border DockPanel.Dock="Top" Margin="0,0,0,10">
          <StackPanel Spacing="6">
            <ItemsControl ItemsSource="{Binding Branches}">
              <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                  <WrapPanel Orientation="Horizontal" />
                </ItemsPanelTemplate>
              </ItemsControl.ItemsPanel>
              <ItemsControl.ItemTemplate>
                <DataTemplate x:DataType="vm:GitBranchRow">
                  <Button Classes="quiet" Margin="0,0,4,4"
                          Command="{Binding $parent[UserControl].((vm:GitViewModel)DataContext).SwitchBranchCommand}"
                          CommandParameter="{Binding}"
                          ToolTip.Tip="この枝へ切り替える">
                    <StackPanel Orientation="Horizontal" Spacing="5">
                      <Path Classes="icon" Data="{StaticResource IconGit}"
                            Width="12" Height="12" VerticalAlignment="Center" />
                      <TextBlock Text="{Binding Name}" VerticalAlignment="Center" />
                      <TextBlock Text="{Binding TrackText}" FontSize="10"
                                 Foreground="{DynamicResource FgWarning}"
                                 IsVisible="{Binding HasTrack}" VerticalAlignment="Center" />
                    </StackPanel>
                  </Button>
                </DataTemplate>
              </ItemsControl.ItemTemplate>
            </ItemsControl>
            <StackPanel Orientation="Horizontal" Spacing="6">
              <TextBox Text="{Binding NewBranchName}" Width="200"
                       PlaceholderText="新しい枝の名前" />
              <Button Content="作って切り替える" Command="{Binding CreateBranchCommand}" />
            </StackPanel>
          </StackPanel>
        </Border>

        <TextBlock DockPanel.Dock="Top" Text="履歴" Classes="title" Margin="0,0,0,6"
                   ToolTip.Tip="行を選ぶと、下にそのコミットの中身が出ます。" />

        <!--
          選んだコミットの詳細。**下に置いて、履歴とは分けない。**
          別のタブへ移すと、行を選ぶたびに視線が画面をまたぐことになる。
          SourceTree と同じ配置。
        -->
        <Border DockPanel.Dock="Bottom" Height="210" Margin="0,8,0,0"
                Background="{DynamicResource PanelBg}"
                BorderBrush="{DynamicResource Divider}" BorderThickness="1"
                CornerRadius="{DynamicResource RadiusMedium}"
                IsVisible="{Binding HasSelectedCommit}">
          <Grid RowDefinitions="Auto,*" Margin="10,8">

            <StackPanel Grid.Row="0" Spacing="3" Margin="0,0,0,6">
              <TextBlock Text="{Binding SelectedCommit.Subject}" FontWeight="SemiBold"
                         TextTrimming="CharacterEllipsis" />
              <StackPanel Orientation="Horizontal" Spacing="10">
                <TextBlock Text="{Binding SelectedCommit.ShortHash}" Classes="mono" FontSize="11"
                           Foreground="{DynamicResource FgKeyword}"
                           ToolTip.Tip="このコミットのハッシュ" />
                <TextBlock Text="{Binding SelectedCommit.Author}" FontSize="11"
                           Foreground="{DynamicResource FgDim}" />
                <TextBlock Text="{Binding SelectedCommit.Ago}" FontSize="11"
                           Foreground="{DynamicResource FgDim}"
                           ToolTip.Tip="{Binding SelectedCommit.When}" />
                <!--
                  親のハッシュ。**アイコンを添える。** ハッシュだけ並べると、
                  自分のものか親のものか見ただけでは分からない。
                -->
                <StackPanel Orientation="Horizontal" Spacing="3"
                            ToolTip.Tip="親のコミット。2 つ以上ならマージ">
                  <Path Classes="icon" Data="{StaticResource IconBranch}"
                        Width="11" Height="11" VerticalAlignment="Center"
                        Fill="{DynamicResource FgDim}" />
                  <TextBlock Text="{Binding CommitParents}" Classes="mono" FontSize="11"
                             Foreground="{DynamicResource FgDim}" VerticalAlignment="Center" />
                </StackPanel>
              </StackPanel>
            </StackPanel>

            <Grid Grid.Row="1">
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="{Binding BodyWidth}" />
                <ColumnDefinition Width="8" />
                <ColumnDefinition Width="*" />
              </Grid.ColumnDefinitions>
              <!-- 説明の全文。1 行目だけでは、なぜそうしたかが分からない -->
              <ScrollViewer Grid.Column="0" IsVisible="{Binding HasCommitBody}">
                <SelectableTextBlock Text="{Binding CommitBody}" Classes="mono" FontSize="11"
                                     TextWrapping="Wrap"
                                     Foreground="{DynamicResource FgDim}" />
              </ScrollViewer>

              <ListBox Grid.Column="2" ItemsSource="{Binding CommitFiles}"
                       SelectedItem="{Binding SelectedCommitFile}"
                       Background="Transparent" BorderThickness="0">
                <ListBox.Styles>
                  <!-- 一覧なので詰める。既定の高さだと 4 件で枠が埋まる -->
                  <Style Selector="ListBoxItem">
                    <Setter Property="Padding" Value="4,1" />
                    <Setter Property="MinHeight" Value="0" />
                  </Style>
                </ListBox.Styles>
                <ListBox.ItemTemplate>
                  <DataTemplate x:DataType="vm:GitCommitFileRow">
                    <Grid ColumnDefinitions="16,Auto,*,Auto" ToolTip.Tip="{Binding Path}">
                      <TextBlock Grid.Column="0" Text="{Binding Mark}" Classes="mono"
                                 FontSize="11" FontWeight="Bold"
                                 Foreground="{Binding MarkColour}" />
                      <!-- 名前を先に。置き場所は後ろに薄く（切れても困らない） -->
                      <TextBlock Grid.Column="1" Text="{Binding Name}" Classes="mono"
                                 FontSize="11" VerticalAlignment="Center" />
                      <TextBlock Grid.Column="2" Text="{Binding Folder}" Classes="mono"
                                 FontSize="10" Margin="6,0,0,0"
                                 IsVisible="{Binding HasFolder}"
                                 Foreground="{DynamicResource FgDim}"
                                 TextTrimming="CharacterEllipsis" VerticalAlignment="Center" />
                      <Button Grid.Column="3" Classes="icon"
                              ToolTip.Tip="このコミットでの、このファイルの変化を見る"
                              Command="{Binding $parent[UserControl].((vm:GitViewModel)DataContext).OpenCommitFileCommand}"
                              CommandParameter="{Binding}">
                        <Path Classes="icon" Data="{StaticResource IconOpen}" />
                      </Button>
                    </Grid>
                  </DataTemplate>
                </ListBox.ItemTemplate>
              </ListBox>
            </Grid>
          </Grid>
        </Border>

        <!--
          見出し。**本体と同じ列幅を使う。** グラフの幅は履歴の形で決まるので、
          両方が同じ値（GraphColumnWidth）を見る。
        -->
        <Grid DockPanel.Dock="Top" Margin="0,0,0,2">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="{Binding GraphColumnWidth}" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="120" />
            <ColumnDefinition Width="100" />
            <ColumnDefinition Width="64" />
          </Grid.ColumnDefinitions>
          <TextBlock Grid.Column="1" Text="説明" Classes="columnhead" />
          <TextBlock Grid.Column="2" Text="日時" Classes="columnhead" />
          <TextBlock Grid.Column="3" Text="作者" Classes="columnhead" />
          <TextBlock Grid.Column="4" Text="コミット" Classes="columnhead" />
        </Grid>

        <ListBox ItemsSource="{Binding Commits}"
                 SelectedItem="{Binding SelectedCommit}"
                 Background="Transparent" BorderThickness="0"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled">
          <ListBox.Styles>
            <!--
              **行の高さを揃え、左右の余白を消す。** グラフの線は行をまたいで
              繋がるので、行の間に隙間があるとそこで途切れる。
            -->
            <Style Selector="ListBoxItem">
              <Setter Property="Padding" Value="0" />
              <Setter Property="Margin" Value="0" />
              <Setter Property="MinHeight" Value="0" />
              <Setter Property="Height" Value="26" />
              <!--
                **中身を行の高さいっぱいに広げる。** 既定は中央寄せで、
                中身はテキストの高さ（17px ほど）にしかならない。上下に
                残った隙間でグラフの線が切れ、丸が浮いて見えていた。
              -->
              <Setter Property="VerticalContentAlignment" Value="Stretch" />
            </Style>
          </ListBox.Styles>
          <ListBox.ItemTemplate>
            <DataTemplate x:DataType="vm:GitCommitRow">
              <Grid>
                <!--
                  **履歴を書き換える操作は置かない。** reset や rebase は、
                  既に送った後だと取り返しがつかない。打ち消したいなら
                  revert（打ち消すコミットを作る）で足りる。
                -->
                <Grid.ContextMenu>
                  <ContextMenu>
                    <MenuItem Header="このコミットへ移る"
                              Command="{Binding $parent[UserControl].((vm:GitViewModel)DataContext).CheckoutCommand}"
                              CommandParameter="{Binding}"
                              ToolTip.Tip="枝から離れた状態（切り離された HEAD）になります">
                      <MenuItem.Icon>
                        <Path Classes="icon" Data="{StaticResource IconGit}" />
                      </MenuItem.Icon>
                    </MenuItem>
                    <MenuItem Header="ここから枝を作る"
                              Command="{Binding $parent[UserControl].((vm:GitViewModel)DataContext).BranchHereCommand}"
                              CommandParameter="{Binding}"
                              ToolTip.Tip="上の「新しい枝の名前」に入れた名前で作ります">
                      <MenuItem.Icon>
                        <Path Classes="icon" Data="{StaticResource IconBranch}" />
                      </MenuItem.Icon>
                    </MenuItem>
                    <Separator />
                    <MenuItem Header="打ち消すコミットを作る"
                              Command="{Binding $parent[UserControl].((vm:GitViewModel)DataContext).RevertCommand}"
                              CommandParameter="{Binding}"
                              ToolTip.Tip="履歴は書き換えません。送った後でも安全に使えます">
                      <MenuItem.Icon>
                        <Path Classes="icon" Data="{StaticResource IconUndo}" />
                      </MenuItem.Icon>
                    </MenuItem>
                    <MenuItem Header="いまの枝に載せる"
                              Command="{Binding $parent[UserControl].((vm:GitViewModel)DataContext).CherryPickCommand}"
                              CommandParameter="{Binding}"
                              ToolTip.Tip="このコミットの変更だけを取り込みます（cherry-pick）">
                      <MenuItem.Icon>
                        <Path Classes="icon" Data="{StaticResource IconToRight}" />
                      </MenuItem.Icon>
                    </MenuItem>
                    <Separator />
                    <MenuItem Header="ハッシュを写す"
                              Command="{Binding $parent[UserControl].((vm:GitViewModel)DataContext).CopyHashCommand}"
                              CommandParameter="{Binding}">
                      <MenuItem.Icon>
                        <Path Classes="icon" Data="{StaticResource IconClipboard}" />
                      </MenuItem.Icon>
                    </MenuItem>
                    <MenuItem Header="説明を写す"
                              Command="{Binding $parent[UserControl].((vm:GitViewModel)DataContext).CopySubjectCommand}"
                              CommandParameter="{Binding}" />
                  </ContextMenu>
                </Grid.ContextMenu>

                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="{Binding GraphWidth}" />
                  <ColumnDefinition Width="*" />
                  <ColumnDefinition Width="120" />
                  <ColumnDefinition Width="100" />
                  <ColumnDefinition Width="64" />
                </Grid.ColumnDefinitions>

                <vm:CommitGraph Grid.Column="0" Row="{Binding Graph}" />

                <!--
                  **横並びの StackPanel に入れない。** 中の要素は幅が無限に
                  取れるので、TextTrimming が効かず、説明が隣の列へはみ出す。
                  札は Auto、説明は残り全部の Grid にする。
                -->
                <Grid Grid.Column="1" ColumnDefinitions="Auto,*" VerticalAlignment="Center">
                  <!-- 枝とタグの札。説明の前に置く（SourceTree と同じ） -->
                  <ItemsControl Grid.Column="0" Margin="0,0,4,0"
                                ItemsSource="{Binding Refs}" IsVisible="{Binding HasRefs}"
                                VerticalAlignment="Center">
                    <ItemsControl.ItemsPanel>
                      <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal" Spacing="3" />
                      </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                      <DataTemplate x:DataType="vm:GitRefChip">
                        <Border Background="{Binding Background}" CornerRadius="8"
                                Padding="5,1" VerticalAlignment="Center">
                          <StackPanel Orientation="Horizontal" Spacing="3">
                            <Path Data="{Binding Icon}" Fill="{Binding Foreground}"
                                  Width="11" Height="11" Stretch="Uniform"
                                  VerticalAlignment="Center" />
                            <TextBlock Text="{Binding Text}" FontSize="10"
                                       Foreground="{Binding Foreground}"
                                       FontWeight="{Binding Weight}"
                                       VerticalAlignment="Center" />
                          </StackPanel>
                        </Border>
                      </DataTemplate>
                    </ItemsControl.ItemTemplate>
                  </ItemsControl>

                  <TextBlock Grid.Column="1" Text="{Binding Subject}" FontSize="12"
                             TextTrimming="CharacterEllipsis" VerticalAlignment="Center" />
                </Grid>

                <TextBlock Grid.Column="2" Text="{Binding When}" FontSize="11"
                           Foreground="{DynamicResource FgDim}" VerticalAlignment="Center" />
                <TextBlock Grid.Column="3" Text="{Binding Author}" FontSize="11"
                           Foreground="{DynamicResource FgDim}"
                           TextTrimming="CharacterEllipsis" VerticalAlignment="Center" />
                <TextBlock Grid.Column="4" Text="{Binding ShortHash}" Classes="mono" FontSize="11"
                           Foreground="{DynamicResource FgKeyword}" VerticalAlignment="Center" />
              </Grid>
            </DataTemplate>
          </ListBox.ItemTemplate>
        </ListBox>
      </DockPanel>
    </Grid>

    <!-- 下の帯 -->
    <Border Grid.Row="2" Classes="statusbar">
      <StackPanel Orientation="Horizontal" Spacing="14">
        <TextBlock Text="{Binding Summary}" VerticalAlignment="Center" FontSize="12" />
        <TextBlock Text="{Binding Message}" Foreground="{DynamicResource FgWarning}"
                   VerticalAlignment="Center" FontSize="12" TextWrapping="Wrap" />
      </StackPanel>
    </Border>

  </Grid>
</UserControl>
